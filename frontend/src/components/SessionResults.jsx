import {
  BarChart, Bar, LineChart, Line,
  XAxis, YAxis, CartesianGrid, Tooltip, Legend,
  ResponsiveContainer
} from 'recharts';

function SessionResults({ sessionId }) {
    return (
        <>
            Come back after play stage finished.
        </>
    )
}

export default SessionResults;
